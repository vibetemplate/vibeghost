import React from 'react'

interface HeaderProps {
  onRefresh?: () => void
}

const Header: React.FC<HeaderProps> = ({ onRefresh }) => {
  // Header组件现在为空，功能转移到功能区
  return null
}

export default Header