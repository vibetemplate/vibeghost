import React from 'react';
import { Empty } from 'antd';

const ConfigView: React.FC = () => {
  return (
    <div style={{ padding: 20, textAlign: 'center', height: '100%' }}>
      <Empty description="应用配置功能正在紧张开发中..." />
    </div>
  );
};

export default ConfigView; 